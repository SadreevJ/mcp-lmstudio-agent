[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "local-ai-dev-system"
version = "0.1.0"
description = "xUdav: local LM Studio workspace, CLI, and MCP-oriented dev layout."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
